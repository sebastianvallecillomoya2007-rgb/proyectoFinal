# Catálogo OpenGames (sin clave de API)
VITE_API_URL=https://opengames.dev/api
# Respaldo del mismo proveedor si el dominio anterior no responde.
OPENGAMES_FALLBACK_URL=https://www.open-source-games.com/api
# Las cuentas, reseñas, deseados y compras usan /api en el servidor local.
